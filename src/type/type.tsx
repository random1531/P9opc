export type Plant = {
    name: string,
    category: string,
    id: string,
    light: number,
    water: number,
    cover: string,
    price: number,
    description: string
}
export type CartItem = {
    name: string,
    price: number,
    amount: number
}